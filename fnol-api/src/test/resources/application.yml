server:
  port: 0
mock-guidewire:
  base-url: http://localhost:9090
fnol:
  orchestrator:
    timeout-seconds: 10
spring:
  datasource:
    url: jdbc:h2:mem:fnol_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
