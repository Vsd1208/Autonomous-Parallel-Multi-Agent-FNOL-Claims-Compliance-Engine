import React,{useState}from'react';import{createRoot}from'react-dom/client';import{ShieldCheck,Upload,Search,Activity}from'lucide-react';import'./style.css';
const API=import.meta.env.VITE_API_BASE_URL||'http://localhost:8080';
type Result=any;
function App(){const[form,setForm]=useState({policyNumber:'POL-AUTO-112233',incidentDate:'2026-08-16',state:'CA',description:'Rear-ended by other driver. Police report filed. Claimant Avery Johnson DOB 01/02/1980 SSN 123-45-6789.',claimantName:'Avery Johnson',claimantDob:'01/02/1980',claimantSsn:'123-45-6789'});const[result,setResult]=useState<Result>();const[busy,setBusy]=useState(false);const[err,setErr]=useState('');
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setErr('');try{const r=await fetch(`${API}/api/v1/fnol/intake`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,evidenceUris:['mock://photo/front-bumper.jpg']})});const j=await r.json();if(!r.ok)throw new Error(j.message||'FNOL failed');setResult(j)}catch(x:any){setErr(x.message)}finally{setBusy(false)}}
const a=result?.branchA,b=result?.branchB;return <main><section className="shell"><div className="top"><div><h1>FNOL Intelligence Console</h1><p>AI-assisted claims decision support with mock Guidewire integration contracts.</p></div><span><Activity size={16}/>Sequential orchestration</span></div><div className="grid"><form onSubmit={submit} className="panel"><h2>FNOL Intake</h2><label>Policy Number<input value={form.policyNumber} onChange={e=>setForm({...form,policyNumber:e.target.value})}/></label><label>Incident Date<input type="date" value={form.incidentDate} onChange={e=>setForm({...form,incidentDate:e.target.value})}/></label><label>State<input value={form.state} onChange={e=>setForm({...form,state:e.target.value.toUpperCase()})}/></label><label>Description<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="upload"><Upload size={18}/> Evidence attached: front-bumper.jpg</div><button disabled={busy}><ShieldCheck size={18}/>{busy?'Processing':'Submit FNOL'}</button>{err&&<p className="error">{err}</p>}</form><section className="panel result"><h2>Claim Result</h2>{!result&&<div className="empty"><Search size={34}/><p>Submit an FNOL to see coverage, reserve, compliance, subrogation, and audit outputs.</p></div>}{result&&<><div className="claim"><strong>{result.claimId}</strong><span>{result.status}</span></div><div className="metrics"><Metric k="Coverage" v={a.coverage.covered?'Covered':'Review'}/><Metric k="Damage" v={a.damageAssessment.severity}/><Metric k="Reserve" v={`$${Number(a.reserve.recommendedReserve).toLocaleString()}`}/><Metric k="Deadline" v={b.deadline.deadline}/><Metric k="Subrogation" v={`${b.subrogation.score} / 100`}/></div><h3>Why</h3><ul>{result.explanation.map((x:string)=><li key={x}>{x}</li>)}</ul><h3>Audit Trail</h3><div className="audit">{b.audit.map((x:any)=><div key={x.eventId}><span>{x.agentName}</span><code>{x.outputHash.slice(0,12)}</code></div>)}</div></>}</section></div></section></main>}
function Metric(p:{k:string,v:string}){return <div className="metric"><span>{p.k}</span><strong>{p.v}</strong></div>}
createRoot(document.getElementById('root')!).render(<App/>);
